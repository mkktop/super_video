# 更新说明

## v0.5.1

超分引擎稳定性修复（四个「任务成功但结果不对/太慢」的隐性问题）与静帧对比升级。

### 超分引擎修复

- 修复 IllustrationJaNai 4x DAT2（彩色插画 · 画质）在 TensorRT 后端输出纯黑图片的问题：transformer 类模型与 TRT 混合精度不兼容，现在会自动改用兼容精度完成出片
- 修复大尺寸图片在 TensorRT 后端异常缓慢的问题（单张数十秒 → 修复后不足 1 秒）：此前 TRT 引擎构建失败会静默降级到慢速路径且无从察觉
- 修复大尺寸图片在 DirectML 后端输出灰蒙蒙/近黑图片的问题：引擎加载时会用一帧彩色探测图自动校验输出，异常时自动切换 512px 分块等兼容方式重试，视频/图片/模型对比全部受益
- 显存暂时不足不再直接报「引擎加载失败」：自动降分块重试、仍不足时回退 CPU 保出片；极少数仍失败的任务，错误信息现在会显示真实原因（如「内存资源不足」），不再是一段看不懂的编码报错

### 静帧对比升级

- 静帧对比默认按 1:1 像素显示：分割线左侧是平滑放大的原片、右侧是原生像素的成片，超分效果一眼可见（此前整图缩放显示，细节被压缩得「看不出效果」）
- 对比画面支持拖动平移、滚轮缩放（光标锚定）、双击在 1:1 与整图适配间切换，底部提供 适配 / 1:1 / 2:1 / 4:1 快捷档位；悬浮放大镜保留

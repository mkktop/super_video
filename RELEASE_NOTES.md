# 更新说明

## v0.4.8

正式版，汇总 v0.4.8-preview.1 / preview.2 两个预览版的全部改进。

### 界面全面换新
- 全新深空主题：层次化背景与卡片、统一的蓝紫品牌渐变（主按钮 / 选中态 / 进度条）、更精致的滚动条与页面切换动效
- 首页重做：新增「480p → 4K」渐清晰动效演示；硬件信息升级为实时仪表——显卡金属渐变型号名 + TensorRT / CUDA 徽标 + 实时显存占用条，处理器 / 内存卡带实时占用条（每 2 秒刷新）；右上角引擎状态改为「● 引擎就绪 │ TENSORRT │ 显卡短名」分段胶囊，不再显示内部代号
- 任务卡新增状态色脊线（运行中 / 完成 / 失败 / 取消一眼可辨）与流光进度条，悬停缩略图可放大预览
- 视频剪切页：未选视频时显示整块拖放引导区（格式清单 + 最近文件一键直达），预览舞台与结果卡视觉升级
- 任务筛选分段控件、新建任务步骤面板、模型市场卡片悬停浮起；对比页、性能页、日志页、设置页全套同步新视觉
- 窄窗口下首页卡片自动换行（显卡独占一行 / 统计与性能环 2×2），不再挤压错乱

### 新功能
- 设置 → 输出位置 新增「超分完成后删除源文件」（默认关闭）：任务成功完成后自动删除该任务的源文件（图片批量任务删除全部输入图）；失败/取消的任务不删，删除不进回收站，开启前请确认不再需要原片/原图
- 源文件被删除或移动后，任务卡的「全页对比」按钮和预览缩略图自动置灰停用（悬停显示原因），不再点进去面对无法播放的对比页
- 设置 → 应用与更新 新增「更新下载源」：自动（默认，GitHub 连不上自动切 R2 备用源）/ R2 优先（国内直连推荐）/ 仅 GitHub，切换后立即按新源重新检查

### 性能与体验
- 队列中相同模型/参数的连续任务跳过推理引擎重复加载（此前 TensorRT 每个任务都要等一次 5-8 秒的加载预热），连跑同款任务明显更快
- 推理与模型对比、剪切导出等辅助 GPU 作业之间增加显存互斥排队，降低大任务与对比同时抢显存导致崩溃的概率
- 界面安装包按页面拆分加载，主程序体积显著减小；任务进度改为增量刷新，长任务列表下更流畅省资源

### 修复
- 修复窄窗口下首页硬件卡挤压、文字竖排与横向滚动条
- 修复新建任务页一处潜在的界面渲染崩溃（内置预设的删除确认触发槽渲染缺陷，特定环境下导致整页白屏）
- 修复模型列表排序在部分情况下失效（已安装的模型没有稳定排在前面的）问题

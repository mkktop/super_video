# 更新说明

## v0.5.2

图片超分支持整个文件夹（含子目录，输出镜像目录结构），图片对比改为全分辨率原图对比；GPU 偶发重置不再让任务失败。

- **图片超分 · 文件夹模式**：新建图片任务可直接选整个漫画文件夹，递归收录子目录（卷/话）里的全部图片，合并为一个批量任务。输出不再散页：按源目录结构镜像到「文件夹名_倍率」目录（如 `manga_x4/ch01/p001.png`），也可整本无损合并出一份 PDF。
- **图片对比 · 全分辨率**：此前对比页展示的是 960px 预览缩略图，源图和超分成片都被压成同一张小图，分割线两侧看不出差异；现在直接用原文件与成品全分辨率对比，1:1 即成片原生像素，锐利与模糊的分界一眼可见。
- **GPU 偶发重置自愈**：极少数情况下任务开始前显卡已被系统重置（错误 887A0005/6/7），此前任务会直接失败；现在会等待驱动恢复后自动重试，仍不行则临时改用 CPU 保住出片，下一个任务自动回到 GPU。
- **首页演示动效修复**：480p→4K 像素重构演示的扫描线自上线起从未动过（属性继承缺陷），且模糊/清晰分界与光标无关；现在扫描线正常往返，光标左侧恒为马赛克、右侧恒为清晰，分界随手柄移动。

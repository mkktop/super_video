# 更新说明

## v0.3.1
老片修复与挂机自动化版本:画面预处理滤镜 / 智能推荐 / 自定义预设 / 队列自动化 /
对比放大镜与分享卡片 / 输出命名模板

### 画面预处理(新建任务 → 输出设置)
- **反交错**:老 DVD / 1080i 隔行片源的超分前置修复,交错纹路不再被放大成锯齿
- **去色带**:修复夜空、暗部渐变里的色彩断层(动画源常见)
- 与断点续跑兼容(滤镜不改变帧数),硬件解码与滤镜的组合会在开跑前实测验证,
  不可用时自动回退软件解码

### 智能推荐
- 选择视频后自动分析内容:判定动画/真人、检测隔行扫描、识别老编码,
  给出模型、倍率与预处理建议,**一键应用**
- 每条建议附判断依据(如"采样 4 帧平坦像素占 72%,判定为动画内容"),
  高清真人源自动避开 8K 编码上限(退到 x2 档)

### 自定义预设
- 新建任务页可把当前全套参数(模型/倍率/编码画质/容器/音轨/补帧/降噪/预处理)
  **保存为「我的预设」**,下次一键调用;不再受限于内置的 4 个预设

### 队列自动化(设置 → 通知与窗口 / 处理时机)
- **队列全部完成后**可自动:系统通知 / 关机 / 休眠——睡前挂队列、跑完自动关机;
  关机/休眠前有 60 秒反悔窗口,期间新任务入队或改设置都会自动撤销
- **处理时机**:立即处理 / 指定时段(如 22:00~08:00,支持跨午夜) / 电脑空闲时
  (键鼠静置指定分钟后开始)——白天不抢机器;只拦"开始下一个任务",
  进行中的任务会跑完(断点续跑安全),挂起期间任务页显示等待原因

### 对比体验升级(任务对照页 / 模型对比)
- **局部放大镜**:对比滑块新增放大镜模式(🔍 按钮),悬浮查看细节差异,
  滚轮调节 1.5~8 倍,分割线与主视图同步
- **分享卡片**:一键生成"处理前/后"对比的**长图 PNG**(上下叠放+信息条)或
  **滑块动图 GIF**(分割线来回扫),晒图即传播

### 输出命名模板(设置 → 输出位置)
- 批量产出文件管理刚需:模板变量 {name} 原名 / {model} 模型 / {scale} 倍率 /
  {res} 输出分辨率 / {date} 日期,如 `{name}_{model}_{scale}`;
  同名冲突仍自动加后缀,绝不覆盖现有文件

### 设置页
- 推理后端展示改为"下一任务将使用"口径:切后端保存后立即反映,
  任务运行中时并排展示当前任务实况所用后端

### 说明
- 预处理滤镜接入全部三条处理管线(流式/分段/分块),续跑任务参数照常带回
- 分享卡片产物在应用内预览,可一键打开所在文件夹取用

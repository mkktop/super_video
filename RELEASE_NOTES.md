# 更新说明

## v0.3.5
任务对比页多帧静帧

### 任务对比页多帧静帧
- 任务的全页对比此前只有一对预览帧（处理时抽的首帧），想看别的画面只能
  切视频模式拖进度；现在静帧模式支持多帧样本，与模型对比页同款交互：
  底部缩略图条点选或 `[` `]` 键切换，自动避开黑场、源与结果严格同时间戳
- 样本数沿用设置 → 对比 → **静帧样本数**（1~8，默认 4）：模型对比创建
  作业时快照，任务对比页打开时按当前值即时构建；改小/改大后下次打开
  自动按新数重建
- 视频结果与**帧序列导出**（PNG/JPG 目录）的任务都支持——序列任务没有
  视频模式可切，多帧静帧是它唯一的逐帧对照入口；图片任务无多帧概念，
  维持单对预览
- 首次打开时后台抽帧（页面显示「正在抽取样本帧…」，期间先看原预览对），
  产物缓存在数据目录，重复打开秒出；设置 → 对比的「清理缓存」现在同时
  清理模型对比切片与任务静帧两类产物（统计口径合并）

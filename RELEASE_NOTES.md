# 更新说明

## v0.5.4

漫画混装双模型新增「分趟处理」开关：两个引擎不再同时占用显存。

- **混装 · 分趟处理（省显存）**：彩色/黑白混装任务可选「先跑完全部黑白页、释放引擎后再加载彩色模型」，显存峰值从「两个模型之和」降到「约单个模型」，小显存机器跑混装整本更稳。趟间重建引擎需几秒、进度按「先黑白后彩色」推进；输出图片与 PDF 始终按原页序，不受影响。不开启则维持原来的双引擎并存模式（分派只换对象、无重建），纯黑白/全彩整本依旧只加载需要的那个模型。

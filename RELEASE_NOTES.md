# 更新说明

## v0.1.16
新建任务独立成页:弹窗改整页,单屏分区表单 + 预设反馈可视化

### 新建任务页
- 「新建任务」从固定 720px 弹窗迁移为独立侧栏页面,侧栏顺序调整为 首页/任务/新建任务/模型市场/视频剪切/性能/日志/设置
- 去掉三步向导,视频/模型/输出设置三个分区一页看完:模型卡片网格全宽 3~4 列,输出设置两列排布(窄窗口自动退化单列)
- 提交按钮吸底常驻;提交成功后自动跳转任务页查看队列
- 填写中途切换到其他页面再回来,草稿不丢(页面常驻挂载)
- 一键预设反馈可视化:点击后平滑滚动到模型区,选中卡片带 ✓ 角标,分区标题常驻「已选 模型 · xN」状态
- 剪切页「去超分」照常预填输入文件,改为直接跳转新建任务页

### 修复
- 多选文件批量入队此前被第一步校验卡住(单文件探测逻辑误用于批量),现已放行

# 更新说明

## v0.3.2
模型扩容版本:对标同类工具补齐两条新模型线 + 修复一个潜伏崩溃

### 新增 Real-CUGAN Pro(动漫·高画质)
- B 站 Real-CUGAN 官方 Pro 版(v3 训练 + SE 注意力),画质比现有 latest 更好,
  速度更慢——追求画质时的动漫新选择
- 覆盖 x2 / x3 两档倍率,降噪提供**保守(默认)/ 不降噪 / denoise 3** 三种
- 注意:与 latest 相同,该模型**仅 TensorRT 与 CPU 后端可用**(DirectML/CUDA
  下存在显存泄漏前科,选择时会明确提示);引擎已自动处理 Pro 特有的
  动态范围压缩,无需手动设置

### 新增 Ani4K v2(2025 新模型,两档)
- **Compact(画质)**:1080p→4K 画质档
- **UltraCompact(极速)**:实时级速度档,长任务快速出片
- G6i2 紧凑架构,任意分辨率源免对齐直接跑;经本产品 GPU 前后处理优化加速

### 修复
- **Real-CUGAN x3 特定分辨率崩溃**:源高度为 45/51/63 等"3 的倍数且为奇数"
  的视频(部分老动画/自制片源)在 x3 倍率下直接报错中断。根因是该模型要求
  边长为 4 的倍数而补边按 3 对齐,现按倍率自动正确补边;x2/x4 输出不受影响

### 说明
- 新模型在**模型页**点击下载后即可在新建任务中使用(共 8 个新权重,
  Ani4K 每档约 1.2~2.4MB,CUGAN Pro 每个约 5MB)
- Ani4K v2 模型按原作者条款(CC BY-NC 4.0)以非商业方式提供

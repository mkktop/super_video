# 更新说明

## v0.4.9

正式版，汇总 v0.4.9-preview.1 的全部改进：界面质感与交互体验全面升级，新增浅色主题与全局命令面板。

### 界面焕新
- 全新「暗夜工作室」背景氛围：极淡的品牌色辉光与细腻噪点纹理，画面更有纵深
- 全站卡片统一：一致的底色、描边与圆角，悬浮时轻轻上浮；统计数字改为 28px 等宽大字，不再跳动
- 首页「480p → 4K」演示升级为会动的「像素重构」动画：一条扫描线 4 秒一轮从左向右扫过，扫过之处马赛克重构为清晰画面
- 任务完成时：卡片边缘闪过一圈成功色描边、状态标签轻弹一下，首页统计数字平滑滚动补间
- 任务对比页无可对比内容时显示统一的分割线插画空状态（处理中 / 无预览两种说明）

### 新功能
- 浅色主题：设置 → 外观 可选 深色 / 浅色 / 跟随系统（随 Windows 深浅色模式自动切换），偏好保存在本机；全站页面浅色下逐一核验无对比度盲区
- 全局命令面板：任意页面按 Ctrl + K，搜索跳转页面、执行常用动作（新建任务 / 打开模型对比 / 打开输出目录 / 切换主题）、直达最近任务（完成的可直接进对比）；设置 → 外观 内附全部快捷键对照表
- 任务页与模型市场新增空状态插画与加载骨架屏，初始化过程中按真实布局占位，不再空白等待
- 排队中的任务悬停时，卡片左侧显示拖拽手柄，暗示可拖动调整顺序

### 体验细节
- 切换页面按导航方向浮动入场：往下点从下方浮入，往上点从上方浮入
- 运行中的任务卡：状态脊线渐变流动、右上角呼吸圆点、帧率与剩余时间淡入刷新
- 标题栏细节：关闭按钮悬停变红底（Windows 惯例）、有新版本时提示按钮呼吸辉光、启动时 logo 扫过一道微光
- 所有装饰动效在系统开启「减少动态效果」时自动静止；视频对比页不加任何画面滤镜，性能优先

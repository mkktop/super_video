# 更新说明

## v0.5.3

漫画超分独立成档，支持彩色/黑白混装整本处理；失败任务改参数重试会回到对应页面。

- **漫画超分 · 独立入口**：侧栏新增「漫画超分」页，选整个漫画文件夹（含子目录）一键整本处理，输出镜像目录结构、默认整本合并 PDF；模型默认只列漫画专用模型（可展开全部）。图片超分页回归散图本职，任务列表新增类型筛选（视频/图片/漫画）。
- **漫画 · 混装双模型**：整本里既有彩页又有黑白页时，可分别指定彩色页模型与黑白页模型——创建任务时逐页自动识别色彩分派（识别结果随任务保存，续跑不重算）。识别对发黄的旧黑白扫描件免疫（按色彩离散度判定，不按偏色深浅）；纯黑白整本不会多加载彩色模型引擎。
- **改参数重试 · 正确回跳**：失败/取消任务的「改参数重试」现在按任务类型跳回对应页面——视频→新建任务、图片→图片超分、漫画→漫画超分，并完整带回原参数（整本文件夹重新载入、混装双模型、格式/分块/PDF 选项）。此前一律进视频向导页。
- 顺手修复：从任务页首次直达目标页时重试参数可能未带入的问题（KeepAlive 页预填时序）。

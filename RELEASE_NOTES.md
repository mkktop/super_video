# 更新说明

## v0.2.2
数据持久化修复版本

### 问题修复
- **更新后数据丢失(重要)**:此前版本的数据目录实际建在了安装目录内部,每次应用更新时都会被旧版卸载程序连带清空,导致模型权重、TensorRT 加速组件、任务历史需要重新下载。本版起数据目录移至安装目录的同级位置,更新与重装不再影响任何用户数据;升级时自动迁移已有数据

### 升级提示
- 请先升级到本版,再重新下载模型/加速组件(升级前下载仍会随旧目录一起被清除);本次重新下载后,后续更新不会再丢失

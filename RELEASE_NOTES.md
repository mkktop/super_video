# 更新说明

## v0.2.4
正确性与可靠性修复版本（全面代码审查落地）

### 视频正确性（重要）
- **补帧颜色修复**:RIFE 补帧的插帧画面此前红蓝通道错配(与正确画面交替出现会闪烁),现已修复;同时补帧引擎自动对非 32 对齐的尺寸做安全填充,任意分辨率不再崩溃
- **尾批丢帧修复**:帧数不能被批量参数整除的视频,每段最多丢 3 帧(音画逐渐漂移),现已修复;分段处理新增段产出帧数校验,异常段落不再被静默固化
- **fp16 转换加固**:模型 fp16 转换改为原子写入,转换中断不再留下损坏文件导致后续任务持续失败

### 稳定性
- **退出语义**:带任务正常退出应用时,进行中的任务按"已取消"收尾(可续跑),不再误报失败并删除半成品
- **孤儿进程清理**:后端被强杀后残留的 worker 进程在下次启动时自动清杀,不再与新任务双写同一文件
- **视频剪切**:新增取消按钮;单个 ffmpeg 卡住不再永久堵死剪切队列(30 分钟看门狗);取消不再被"硬编回退"吞掉
- **UI 容错**:后端连接失败时显示明确错误与重试按钮,不再永久卡"正在连接后端服务";快速切换文件不再显示过期信息

### 安全
- **本地 API 鉴权**:后端接口增加随机令牌校验,浏览器中的恶意网页无法再直接调用本机接口(读写文件/删除模型等)

### 性能
- **GPU 校验形状修正**:模型首次加载的包装校验改用源视频真实尺寸,避免小尺寸试探拖慢后续真实推理(与 v0.2.3 同源的 DML 特性问题)

### 其他
- 更新检查改用语义化版本比较,远端版本更旧时不再提示"可更新"
- mkv 容器的音轨默认无损拷贝(此前强制转 AAC,少一代有损)
- 发布流程增加测试门禁与版本号一致性校验;依赖版本锁定;CI 增加前端类型检查与构建

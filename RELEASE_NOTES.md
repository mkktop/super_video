# 更新说明

## v0.2.11
稳定性与图片工作流版本:MKV 尾段误报修复 / 批量图片合并 PDF / 对比静帧样本数可调

### 视频超分
- **修复 BDRemux MKV 尾段误报「解码异常」任务失败**:部分蓝光原盘 MKV 的容器
  duration 元数据偏大(实测剧场版多估约 247 帧/10 秒),超分跑到影片真实结尾时
  被误判为解码异常硬失败;现在解码器干净收尾会被识别为真实片尾——接受短尾段、
  记录真实总帧数,任务正常完成,任务卡进度按实测帧数显示(如 139566/139566
  而非钉死在预估值)

### 图片超分
- **批量任务可勾选「合并输出 PDF」**:全部结果按处理顺序无损封装成一份
  PDF——PNG 结果逐像素一致嵌入,JPG 结果按原文件字节直接嵌入不做二次压缩;
  逐张图片文件仍照常保留,PDF 与图片同目录同命名系,不覆盖现有文件
- 页面尺寸跟随图片分辨率,超出 PDF 规格上限自动等比缩放,画质不受影响
- 合并失败时任务显式报错,已产出的图片文件不受牵连

### 模型对比
- **静帧样本数可调(1~8 张)**:设置页「对比」卡片新增静帧样本数,样本越多
  对比越全面,产物占用也越大;创建对比时快照取值,进行中的对比不受影响,
  手改配置文件越界自动钳回合法范围

### 内部
- PDF 封装为零依赖手写实现(PNG 行预测器+Flate / JPEG 直嵌 DCTDecode),
  测试内带独立解码侧逐字节验证无损性
- 测试 210 项全绿 + 1 跳过(新增尾段 EOF 4 用例 + PDF 合并 6 用例 + 静帧样本数 3 用例)

# 更新说明

## v0.2.6
新功能版本:模型对比

### 新功能:模型对比
面对模型市场里十来个模型不知道选哪个?现在可以拿同一段素材实测:
- **素材二选一**:截取一段视频(默认 6 秒,上限 20 秒)或一张图片;视频剪切页选好精确入出点后可直接「拿这段对比模型」跳转,区间自动带过去
- **多模型并测**:勾选 2~6 个模型(倍率自动取共同支持档),未安装的模型会当场下载;逐模型串行处理,实时显示各自进度与 fps
- **与原版分割线对比**:结果页与任务对比页同款交互——拖动分割线左右对照原版,按钮或数字键 1~6 一键切换模型,同一位置连续横跳比较
- **静帧 + 成片双模式**:视频素材可在中点静帧(像素级)与成片同步播放(看动态闪烁)间切换;每个模型的速度/耗时/分辨率一目了然
- **选定即用**:看完点「用此模型处理完整视频/图片」,参数直达对应页面预填

### 修复
- 图片预览修复:CSP 策略 img-src 未放行本地文件协议,导致图片对比预览与图片超分缩略图墙不显示(视频不受影响)
- 图片超分、模型对比的取消均保留已完成的结果文件

### 内部
- 对比作业与任务队列独立(后台线程,不占任务位、不写任务库);引擎加载复用任务同款公共函数(fp16 补转/TRT/显存自动降档)
- 测试 176 → 182 项(对比创建校验/图片视频链路/取消语义/资产白名单)

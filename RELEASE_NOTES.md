# 更新说明

## v0.1.17
超分速度提升 3.6 倍:前后处理 GPU 化 + 管线并行,同画质逐位一致

### 性能(1080p→4K,AnimeJaNai V3 HD L2 实测)
- 超分管线速度 5.7fps → 20.2fps(3.6 倍),GPU 利用率 27% → 73%
- 推理引擎自动启用 uint8 直进直出图手术:每帧的通道转置/归一化/裁剪/转码全部搬到 GPU 执行,CPU 侧零浮点开销,单帧推理 138ms → 37ms
- 解码/推理/编码三阶段并行流水:读下一帧与写上一帧藏进推理时间,再提速 30%
- 输出画质与原路径逐位一致(跳过一次 fp16 输出量化,保真只高不低);每个模型首次使用时自动逐位校验,不一致自动回退原路径,任何环节失败不影响任务
- 各档模型与分辨率普遍受益;30fps 视频现在约 1.5 倍时长出片(此前约 5.3 倍)

### 修复
- 任务卡误显「错误信息」:引擎状态日志(如"u8 包装生效")不再被当作任务错误展示;错误信息区仅在任务失败时显示,成功任务运行期日志一律清除

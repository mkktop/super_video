# 更新日志

## v0.1.10
对比页新增视频模式：拖动进度条对比任意位置

- 对比页新增"静帧 / 视频"切换：视频模式下源片与超分片双路同步播放，拖动分割线擦除对比，操作与静帧模式一致
- 底部进度条可拖到任意时刻，两路画面逐帧对齐（超分片为主时钟），循环播放方便反复观察
- 修复 v0.1.9 剪切页视频预览无法加载的问题（本地视频协议路径拼接错误，盘符前多了一个斜杠）
- 本地视频协议自实现 Range/206 响应，大视频文件任意位置拖动秒级响应
- 源视频为浏览器不支持直接播放的格式（如 MKV/AVI）时，视频模式会提示改用静帧对比

## v0.1.9
新增视频剪切：剪出片段直接去超分

- 侧栏新增"视频剪切"页：页内预览视频、播放中一键设入点/出点（也可拖区间滑条精细调）
- 三种剪切方式：智能（推荐，起点帧精确：只转码头部几秒+其余无损复制，快）/ 快速无损（秒级完成，起点自动对齐关键帧）/ 精确转码（任意源兜底）
- 剪切完成点"去超分"直接打开超分向导并自动填好剪切后的文件，选模型入队即可
- 完整片段剪切不走重编码（智能/快速模式），画质零损失

## v0.1.8
自定义输出分辨率 + 分块高级选项

- 新建任务的"输出分辨率"支持两种模式：按倍数（原有）或自定义精确宽高（如 720p 直接出 1080p、1080p 出 1440p）
- 自定义模式自动选用能覆盖目标的最小原生倍率超分（能 x2 就不 x4，速度最快），再 lanczos 高质量缩放到目标尺寸；不支持的目标（超出模型上限/低于原分辨率）会明确提示
- 新增"高级选项"面板：可手动设置分块大小，显存不足或大分辨率卡顿时调小即可
- 无独显电脑兼容性验证（DirectML 失败自动回落 CPU）、睡眠唤醒后任务继续、跨版本升级链路均已实测加固

## v0.1.7
模型下载支持自定义代理

- 设置页新增"模型下载"：可填本地代理地址（如 Clash 的 http://127.0.0.1:7890），下载模型不再依赖系统代理配置，GitHub 下载慢/失败可明显改善
- 提供"直连"选项用于排查；保存后立即生效，无需重启

## v0.1.6
任务队列拖拽排序 + 任务断点续跑

- 排队中的任务可以直接拖拽调整顺序（拖到运行中的任务上 = 插到队首）
- 任务被取消、崩溃或重启应用后，卡片上点"继续"即可接着跑，不再从头开始
- 任务列表改为"运行/排队在前、历史在后"的清晰分组展示

## v0.1.5
更新方式改进：发现新版本后不再自动下载，改为手动确认

- 设置页"检查更新"发现新版本后，点击"下载更新"才下载（带进度条），完成后点"立即重启"静默安装，不再悄悄占用流量、不再弹窗打断
- 更新说明数据源改为仓库根 RELEASE_NOTES.md，发版不再依赖 tag 附注
- 发布流水线简化：安装包与更新索引统一上传，去掉旧的收敛/补写正文步骤

## v0.1.4
退出时关闭后端并清理残留，标题栏显示版本号

- 关闭应用时后端一并退出（此前可能残留后台进程）
- 启动时自动清理上次异常退出留下的后端进程
- 顶部标题栏显示当前版本号

## v0.1.3
修复部分电脑上无法连接后端的问题

- 启动端口扫描误把机器上其他程序当成后端导致连接失败，已修复
- 首次启动等待时间加长，兼容杀毒软件全量扫描较慢的机器

## v0.1.2
修复自动更新：更新包差分下载错误、手动检查失败、更新安装弹向导

- 检查到新版本后下载到的可能仍是旧版安装包（差分下载缺陷），已修复
- 设置页"检查更新"按钮报错已修复
- 点击"立即重启"后不再弹出安装向导，静默完成更新

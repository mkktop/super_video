# 更新说明

## v0.2.1
修复与界面优化版本

### 问题修复
- **应用更新状态**:下载完成新版本安装包后,切换页面再返回设置页不再丢失"已下载待安装"状态,下载进度也不会因页面切换中断显示
- **推理后端显示**:切换到 TensorRT 后,设置页"当前后端"立即如实更新(此前始终显示 DirectML,实际任务不受影响)
- **任务删除响应**:删除任务后列表即时刷新(此前最长延迟 8 秒)
- **磁盘垃圾清理**:删除任务时自动清理其临时工作目录与预览文件;启动时自动清扫历史遗留的孤儿目录(取消但未删除的任务目录仍保留,供"继续"使用)
- **模型下载进度**:连接中断重连后自动纠正残留的进度显示
- **兼容性**:修复非中文 Windows 系统下任务子进程输出编码错误

### 界面优化
- **设置页全新布局**:宽窗口两列、全屏三列铺满显示,不再挤在窗口中央;"当前后端"独立状态行并显示回退原因;更新状态标签一目了然(已是最新/可更新/已就绪);设备信息改为对齐网格;后端设置修改后出现"未保存"提示
- **首页新增实时性能**:底部展示 CPU / 内存 / GPU / 显存四枚实时仪表,并可一键跳转性能页查看趋势

# 更新说明

## v0.1.21
修复更新后模型和 TensorRT 组件丢失的问题

### 修复
- **更新会清空模型/组件等数据的问题**:此前模型权重、TensorRT 加速组件、设置与缓存都存放在安装目录内,而更新流程会先运行旧版卸载程序、清空整个安装目录,导致这些数据被连带删除(需重新下载)
- 现在数据统一存放在安装目录旁的独立数据目录(`super_video_data`),更新、重装、卸载应用都不再影响它
- 升级本版后,若旧位置还有数据会自动搬移过去

### 重要提示
- 从 v0.1.20 升级到本版时,旧版卸载程序仍会清空一次安装目录——**如果尚未重新下载模型/组件,建议直接升级,不要先下载**;升级完成后再下载,之后更新永不丢失

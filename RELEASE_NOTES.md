# 更新说明

## v0.5.5

全页对比的接缝修复：滑动条拖到边上时，左右两侧的画面分割线不再偏移。

- **对比滑块接缝修复**：此前在放大/平移视图下拖动分割线，画面切换的边界
  与竖线本身会越偏越远（离中心越远偏差越大）——两套坐标系混用所致。
  现在裁剪边界与竖线在任何缩放/平移下都严格对齐，放大镜区域同步修正。
- **CI 质量加固**（不影响使用）：前端单测纳入每次提交检查，发布流程
  的类型检查从「静默空跑」修正为真正执行。

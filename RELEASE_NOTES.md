# 更新说明

## v0.1.14
对标 VSET 补差距:模型市场扩容 + TensorRT 后端 + 编码输出选项 + 运行信息面板;新增性能采样页

### 模型市场扩容
- Real-CUGAN 降噪档补全:不降噪 / denoise 1 / 2 / 3 + 保守模式(x2/x3/x4 全覆盖),
  变体按需下载(任务用哪档下哪档,不再全量下载)
- 新增 AnimeJaNai V2 / V3 HD 各三档(L1 极速 ~ L3 画质,1080p→4K 动漫社区口碑模型);
  引擎支持原生 fp16 模型(V2 系即 fp16 导出)

### 推理与编码
- 设置新增 TensorRT 后端(实验性,N 卡最快;引擎缓存二次运行免重建,组件缺失自动回退 CUDA/DirectML)
- 编码器新增 AV1(NVENC 硬编 + SVT-AV1 软编)与 AMF 硬编(A 卡)
- 封装容器可选 MP4 / MKV / MOV
- 音轨可选原样保留 / AAC / FLAC 无损(MKV)
- 字幕轨保留:MKV 原样保留全部字幕(含图形字幕),MP4/MOV 文本字幕转 mov_text

### 界面与其他
- 首页运行卡显示处理帧率与预计剩余时间
- 新增性能采样页:CPU / 内存 / GPU 占用趋势图(2 秒一拍,最近 1 小时),
  运行任务时叠加任务进程(worker+ffmpeg)CPU/内存曲线,侧边栏常驻占用小字,
  设置页可关闭后台采样
- 服务日志独立成页:最近 300 行、3 秒自动刷新、错误行红色高亮、
  自动滚动不打断向上翻阅(设置页不再堆日志卡)
- 模型市场改为自适应网格卡片,宽窗口多列排布,一屏浏览更多模型
- 视频剪切页全屏跟随(预览随窗口放大),剪切方式固定为精确转码(帧精确),移除模式选择
- 设置页响应式:窗口宽时居中,窄时控件优雅换行,过窄出横向滚动不再挤压
- 修复侧边栏「首页」图标旁多渲染一个齿轮图标的问题
- 修复 Real-CUGAN 变体 fp16 惰性转换产生坏权重的问题(转换前检查模型标记)
- 测试 76 → 99 项(另 1 项按环境跳过)

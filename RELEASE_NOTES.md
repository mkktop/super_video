# 更新说明

## v0.4.4

### 修复：界面崩溃/强杀后重启可能一直「初始化失败」
- 修复界面崩溃或被强制关闭后重新打开，可能永远提示连接不上后端服务的问题
  （此前需在任务管理器手动结束 sidecar.exe 才能恢复）。现在重启会自动换发
  新令牌接管既有服务，正在处理的任务不受影响
- 修复有任务正在处理时点关闭按钮不弹「退出将中断任务」确认对话框的问题
  （同一根因：应用内部访问本地服务未携带令牌，被 401 拒绝）

### 修复：配置文件编码异常导致任务报「引擎加载失败 UnicodeDecodeError」
- 配置文件被外部程序改存成 GBK 等非 UTF-8 编码后（如用记事本手改后另存），
  超分任务会以「引擎加载失败 UnicodeDecodeError: 'utf-8' codec can't
  decode byte ...」失败。现在读取自动兼容此类文件，配置内容完整保留，
  下次在界面保存设置时自动转回标准编码

# 更新说明

## v0.2.9
对比与剪切全面强化版本:全屏对比 / 剪切直达对比 / 对比缓存管理

### 模型对比
- **对比舞台一键全屏**:静帧/成片画面铺满整个显示器,数字键切模型、拖分割线照常可用,ESC 退出
- **「成片」对比不再误报格式问题**:产物是边跑边落盘的,此前刚切到成片的一瞬间文件未就绪会被
  永久误标为"容器/编码不支持";现在自动重试等文件出现,期间提示"成片还在生成中"
- 任务结果的全页对比页同样支持全屏(头部右侧按钮)

### 视频剪切
- **「剪切并去对比模型」**:实际剪切落盘后自动带着产物进入模型对比页,选好模型即可并排试跑;
  结果卡上同时保留「去超分」「去对比模型」手动按钮,任一环节都可续接
- **最近文件快捷切换**:页面顶部「最近」芯片一键载入看过的视频,不必重开文件对话框
  (与新建任务页共用一份最近记录)
- **移除当前视频**:一键回到初始空状态(剪切进行中时禁用防误触)
- **整页拖拽换片**:视频文件直接拖进窗口即载入,与新建任务/图片超分页同款交互
- 修复切换到无法读取的视频时,上一部的区间与输出路径残留问题
- 切页后预览视频自动暂停(不再后台播声音)

### 对比缓存管理
- 设置页新增**「对比缓存」**:显示对比切片/成片的磁盘占用与作业数,一键清理(带确认)
- 有对比正在进行时清理会被拒绝,防止删到正在写入的文件
- 对比产物此前只增不删,长期使用可在此回收空间;清理不影响任务输出与剪切文件

### 超分性能日志
- 新增超分任务性能分析日志(设置 → 超分性能日志开启),完成的任务可查看瓶颈分析
- 任务卡入口直达,定位解码/推理/编码哪一段吃时间

### 跨页跳转修复
- 修复 KeepAlive 常驻页面的跨页预填失效:剪切页带区间跳模型对比、对比页"用此模型"跳
  图片超分,此前第二次及以后跳转会静默失效,现全部修复

### 内部
- 页面新增 assets.d.ts 资源类型声明(静态图片导入的类型基础)
- 测试 194 项全绿 + 1 跳过(新增对比缓存统计/清理/运行中拒绝/路由遮蔽用例,性能日志用例)

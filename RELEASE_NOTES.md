# 更新说明

## v0.4.8-preview.2

预览通道版本。本版是一次全面的界面视觉升级——功能与布局不变，深色主题整体换新。

### 界面换新
- 全新深空主题：层次化背景与卡片、统一的蓝紫品牌渐变（主按钮 / 选中态 / 进度条）、更精致的滚动条与页面切换动效
- 首页重做：新增「480p → 4K」渐清晰动效演示；统计卡换新图标并带悬浮动效
- 硬件信息区升级为实时仪表：显卡卡展示金属渐变型号名、TensorRT / CUDA 徽标与实时显存占用条；处理器 / 内存卡带实时占用条（随任务负载每 2 秒刷新，与性能环同色语义）
- 右上角引擎状态改为分段胶囊：「● 引擎就绪 │ TENSORRT │ RTX 4070 Ti」——后端不再是 trt 这类内部代号，显卡显示短名
- 任务卡新增状态色脊线（运行中 / 完成 / 失败 / 取消一眼可辨），运行中进度条带流光动效，悬停缩略图可放大预览细节
- 视频剪切页：未选视频时显示整块拖放引导区（格式清单 + 最近文件一键直达），预览舞台与结果卡视觉升级
- 任务筛选改为分段式控件；新建任务 / 图片超分的三个步骤包进面板卡，模型选中态更醒目；模型市场卡片悬停浮起
- 模型对比的分割线手柄、性能页、日志页、设置页同步新视觉语言

### 修复
- 修复新建任务页一处潜在的界面渲染崩溃（内置预设的删除确认触发槽渲染缺陷，特定环境下导致整页白屏）

# 更新说明

## v0.4.5

### 数据安全
- 删除任务记录 / 模型权重 / 自定义预设前增加二次确认，误点不再直接删
- 输出文件已存在、或已有排队/运行中的任务将写入同一路径时，先弹窗确认「覆盖并继续」，不再静默覆盖已有文件（自动命名的文件仍会自动避让加后缀）

### 任务队列
- 任务页新增搜索框：按文件名 / 输出路径 / 模型即时筛选
- 任务页新增批量选择：勾选多条后一次取消 / 删除 / 继续（重新入队）

### 选型与试跑
- 新建任务页新增「先试跑 20 秒」：用当前模型快速跑片头片段，与原片对比满意后再入队全片
- 模型对比页新增「总览」网格：源与各模型同帧并排，一眼比完再进分割线细看；单个模型也能直接跑
- 图片超分：个别图片浏览器无法预览时显示占位，不再出现破图，不影响处理

### 界面与细节
- 页面切换增加淡入过渡；视频信息读取中显示骨架占位，不再只有一行文字
- 媒体信息卡新增位深 / 隔行扫描 / 可变帧率提示（隔行源会建议开启反交错）
- 引擎设置新增「CPU」选项——GPU 显存异常时的兜底通道（此前错误提示让用户切 CPU，设置里却没有这个选项）
- 主题色引入统一 CSS 变量层（逐步替换硬编码颜色，为后续主题化铺路）

### 修复
- 修复 v0.4.0 起 CUDA / TensorRT 后端探测误报「缺少校验用模型」、实际回落 DirectML 的问题——装了 TensorRT 组件的用户升级后即可恢复加速
- 修复多权重模型（Real-CUGAN / MangaJaNai 等）下载进度条到不了 100%、中途归零重爬的问题
- 引擎加载失败时完整错误堆栈写入日志，排障不再只能靠一行错误猜

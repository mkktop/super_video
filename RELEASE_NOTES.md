# 更新说明

## v0.3.6
修复字幕静默丢失，字幕保留默认开启

### 修复字幕静默丢失，字幕保留默认开启
- 真实工单：BDRemux 源（三音轨 + 两条 PGS 字幕）超分出的 MKV 音轨全在、
  字幕轨 0 条。根因不是映射逻辑，而是「保留字幕」三路合围永远不生效：
  前端开关默认关（音轨却默认全保留）、批量模式不逐文件探测导致开关
  根本不显示（默认值即终值）、应用预设还会静默把开关重置为关
- 现在**字幕保留默认开启**，与音轨默认行为对齐：MKV 原样保留全部字幕轨
  与内嵌字体；MP4 文本字幕自动转为 mov_text 封装
- 源含图形字幕（PGS/DVB 等）但容器选了 MP4/MOV 时，任务日志会明确说明
  该容器装不下、本次不保留字幕，并提示改用 MKV 即可保留——不再静默丢轨
- 用户预设现在会记住字幕偏好；旧预设不含该字段时保持当前选择，不再
  强制重置为关
- 不想保留字幕可以显式关闭，显式关闭的设置不受本次默认值变化影响

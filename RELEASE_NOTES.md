# 更新说明

## v0.1.18
TensorRT 加速正式版:N 卡用户可在设置页一键安装加速组件,推理速度再翻倍以上

### TensorRT 加速组件(可选)
- 设置页新增「TensorRT 加速组件」卡片:一键下载安装(约 1.5GB,含 GPU 版 onnxruntime 与 CUDA/TensorRT 运行库),下载与解压进度实时可见,卸载即回退 DirectML
- 安装后配合推理后端选「TensorRT」:1080p→4K 实测约 20fps → 50fps(2.5 倍,较提速前累计近 9 倍),超过实时出片
- 组件按显卡架构自动匹配(如 RTX 40 系 sm89 / RTX 50 系 sm120),自动下载对应的引擎编译资源;非 NVIDIA 显卡或未安装组件不受任何影响,推理照常走 DirectML
- 基础安装包体积不变,组件仅在需要时下载;首次用新模型/新分辨率超分时 TensorRT 引擎需编译 1~2 分钟(之后秒级加载),任务日志有明确提示

### 其他
- 升级后旧版后端自动替换:此前从旧版本升级可能继续复用残留的旧后端进程导致新特性不生效,现会比对版本,空闲时自动结束旧进程拉起新版

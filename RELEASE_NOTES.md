# 更新说明

## v0.1.20
双路并行加速:两个进程分段同时推理,配硬编码器实测速度接近翻倍

### 双路并行(设置页开关)
- 设置页「推理后端与精度」新增「双路并行加速」开关:把视频分段交给两个进程同时推理,吃满闲置的 GPU
- 配合硬编码器(NVENC)实测接近 2 倍(1080p→4K,42 → 77.5fps);使用软编码(x264/x265)时 CPU 先饱和,提速有限,开关文案已如实标注
- 显存占用约翻倍,低显存显卡或遇到异常时关闭即可
- 短视频自动回落单路(分段不足 8 段时不并行,双倍引擎加载在小片上反而更慢),无需手动判断
- 取消/失败时两个进程一起结束;断点续跑对双路分段同样有效

### 建议
- NVIDIA 显卡追求最快:推理后端 TensorRT + 编码器 H.264(NVENC) + 双路并行,1080p→4K 可达约 77fps

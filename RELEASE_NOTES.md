# 更新说明

## v0.1.11
新增图片序列输出：超分结果逐帧导出为图片

- 新建任务的"输出格式"三选一：视频 / PNG 图片序列（无损）/ JPG 图片序列（小体积）
- 整个视频超分后的所有帧导出到文件夹，按 000001.png 起的帧号连续命名（补帧任务自动按翻倍后的帧数编号）
- 支持断点续跑：中断后点"继续"只补缺失的帧段，已导出的图片不会被重写
- 两条推理管线（DirectML/ONNX 与 CUDA/PyTorch）均支持；PNG 无损保存，JPG 高质量压缩
- 图片模式下输出改为选择文件夹，界面会提示将导出的总帧数

# 更新说明

## v0.2.10
对比质量与观看体验修正版本:静帧多样本 / 静帧避黑场 / 成片帧同步

### 模型对比
- **静帧扩展为 4 帧样本**:不再只看单一静帧——对比片段均匀取 4 帧,舞台下方缩略图
  一键切换,也可用 `[` `]` 键换帧;切换模型保持当前帧,同帧比不同模型的差异
- **静帧自动避开黑场**:此前固定取片段中点,恰好落在转场黑场或淡入淡出时会拿到
  一张黑图;现在每帧都会检测亮度,黑了就就近换一帧可用的
- 源画面与各模型结果严格取同一时间戳,不再出现左右画面差一帧的情况
- **修复成片对比两路画面偶尔对不上帧**:播放中源/超分两路偶发错开 1~3 帧
  (启动时差、缓冲恢复后的残差),现在按帧级对齐持续校正,轻微偏差平滑追赶
  不再跳帧,大偏差(拖动进度条/循环重播)一步对齐

### 内部
- 静帧产物移入作业目录 stills/ 子目录,资产路由支持帧索引;对比缓存统计与
  清理自动覆盖新目录
- 测试 197 项全绿 + 1 跳过(新增四段避黑采样/全黑素材兜底/同时间戳端到端用例)

# 更新说明

## v0.1.12
修复视频预览：索引在文件末尾的大视频也能正常预览了

- 剪切页/对比页预览改为浏览器原生直接读本地文件（file://）：索引（moov）在文件末尾的大视频（常见于动画压制，几 GB 的 MP4）此前会黑屏，现在秒开、可任意拖动进度条，且不占任何额外磁盘
- 中文文件名与含特殊字符的路径均可正常预览
- 浏览器确实解不了的格式（AVI/WMV 等老容器）会显示明确提示：无法预览不影响剪切，仍可用滑条和入点/出点数字设置区间
- 修复"检查更新"弹窗更新说明显示 HTML 标签的问题，现在显示干净的文字列表

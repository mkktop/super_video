# 更新说明

## v0.2.8
体验升级版本:完成通知 / 关闭到托盘 / 多音轨全保留

### 任务完成提醒与托盘
- **任务完成/失败系统通知**:处理完成弹系统通知并闪烁任务栏图标,点击直达任务页
  (仅在窗口未聚焦时打扰;可在 设置 → 通知与窗口 关闭)
- **关闭时最小化到托盘**(设置 → 通知与窗口):点关闭按钮隐藏到系统托盘,任务继续
  处理、完成通知照常弹出;托盘左键还原窗口,右键菜单可退出应用
- **关窗确认**:有任务正在处理时直接退出会先弹确认,提示下次启动可续跑,防止误关丢进度
- Windows 任务栏图标上实时显示当前任务的进度条

### 多音轨与章节全保留
- **多音轨源不再丢轨**:此前多音轨视频只保留第一条音轨(如国语/粤语双轨、解说+原声),
  现在全部音轨原样保留
- 音轨「自动」模式更聪明:MP4 输出逐轨决定直拷或转 AAC(一条不兼容不再拖累整片重编码);
  MKV 输出全轨无损直拷
- 章节标记(片头/片尾跳转点)与 MKV 内嵌字体附件(ASS 特效字幕依赖)一并继承
- 新建任务时封装容器默认跟随源文件格式(.mkv→MKV、.mov→MOV 等)

### 任务管理
- 任务列表新增状态筛选(全部/进行中/已完成/失败)与「清理已完成」一键清空
- 失败/取消的任务新增「改参数重试」:原任务的模型、倍率、编码等参数自动带入新建页,
  调整后重新入队;另可从任务卡直接打开输入文件所在文件夹
- 排队任务悬停显示 ↑↓ 按钮微调顺序(拖拽之外的补充)

### 界面与交互
- 新建任务/图片超分支持把文件**直接拖进窗口**入队;新建任务页新增「最近」文件快捷条
- 选择视频后显示首帧缩略图,一眼确认选对了文件
- 视频剪切、模型对比、新建任务、图片超分四个页面切换时**不再丢失**已填内容与进行中的作业
- 剪切页手动改过的输出路径不再被区间调整自动覆盖
- 图片超分模型选择升级为与其他页一致的卡片网格
- 日志页新增关键字过滤;侧栏任务项显示失败数红色角标
- 首次使用显示三步上手引导;界面小字对比度提升,长读更省眼

### 内部
- 新增设置项:notify_task_done(任务通知)、close_to_tray(关闭到托盘)
- 测试 185 项全绿 + 1 跳过(新增音轨逐轨编码/字体附件/章节继承用例)

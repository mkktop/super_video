# 更新说明

## v0.2.12
性能版本:新增硬件解码(NVDEC / D3D11VA),超分前解码不再固定 CPU 软解

### 视频超分
- **新建任务页输出设置新增「解码器」**:软件解码(默认)/ 硬件解码 NVDEC
  (NVIDIA 显卡)/ 硬件解码 D3D11VA(AMD / Intel 显卡),超分前的视频解码
  交给显卡完成,降低解码阶段 CPU 占用
- **可用性按所选视频实测**:选择单个文件时逐项真解码验证(含源编码支持
  判断),不支持的选项自动禁用并标注原因;换文件后当前选择不再支持时自动
  回落软件解码;批量任务按设备能力兜底,个别文件不支持时运行时自动回退
  软解并在任务日志说明,不影响任务成败
- 老编码(msmpeg4 / flv 等)硬件解码形同虚设且无任何报错,已在选项层面
  挡掉,不会再出现"开了硬解实际还在软解"的假加速
- 运行环境变化(换卡 / 驱动更新)导致硬解失效时,任务回退软解继续跑,
  不会失败;分段续跑的断点完整性不受影响(开跑前对真实源预验证)
- 设置页设备信息卡新增「硬件解码」行(NVDEC / D3D11VA 驱动检测)
- 超分性能日志头部记录本次任务使用的解码器

### 内部
- 解码器为每任务独立参数,创建端点做白名单与源编码矩阵校验(与硬件编码
  不可用即拒同款语义);三条管线(流式 / 分段 / 扩散分块)全接入
- ffmpeg -hwaccel 为尽力而为语义,初始化失败会静默回退软解且退出码仍为
  0——可用性判定除退出码外还检查 stderr 回退警告,三态齐全
- 测试 213 项全绿 + 7 跳过(test_gpu_decode 重写 9 用例:参数位置 / 三管线
  透传 / 编码矩阵门控 / 回退警告检测 / 真机实测 / 端到端硬解冒烟 / 创建
  端点校验 / probe 可用性 map)

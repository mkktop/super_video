# 更新说明

## v0.2.5
功能扩展版本:图片超分 + 全局输出目录 + 输出命名优化

### 新功能
- **图片超分**:新增独立标签页,支持 PNG / JPG / WebP / BMP / TIFF 单张或批量超分。批量合并为一个任务处理——只加载一次模型循环跑完全部图片,坏图自动跳过不拖垮整批,进度按张数推进;支持 PNG 无损与 JPG(可调质量)输出,手机竖拍照片自动按 EXIF 方向转正
- **全局输出目录**:设置页新增「输出位置」,超分任务与视频剪切的结果统一保存到指定目录(目录不存在自动创建);留空则沿用每个源文件所在目录
- **输出命名优化**:视频与图片统一规则——输出目录没有同名文件时直接沿用原文件名,同名冲突(含源文件本身)时自动改用「原名_倍率」后缀,绝不覆盖任何现有文件;同目录换封装(如 MKV→MP4)可得到干净的原名输出

### 界面
- **设置页重排**:统一卡片风格(标题+副题+行式内容),宽屏固定两列配平布局,不再随窗口宽度挤出多列窄卡
- 新建任务页的输出路径预填与后端命名规则保持一致;手动修改过的路径不再被自动填充覆盖

### 修复
- CI 前端类型检查空壳修复:此前指向无文件的根 tsconfig,渲染进程从未被真正检查;现已显式检查主进程与渲染进程两个子项目,并修复由此暴露的 6 处存量类型错误
- 修复测试套件被本机运行中应用污染的问题(API 令牌文件导致全部接口测试 401,取决于机器状态)
- 修复视频剪切页注册处的引号笔误

### 内部
- 测试 156 → 176 项(输出目录路由 / 图片超分创建与批量循环 / 命名冲突回退)
- worker 引擎加载逻辑抽为公共函数,视频与图片任务共用(fp16 补转 / TRT 激活 / 显存自动降档 / 真实尺寸预热)

# 版本纪律：下界 = 当前已验证可用的版本，上界 = 锁死大版本防破坏性更新
# （CI 绿、Release 坏的事故面：PyPI 上游大版本兼容性断裂）。
# 升级时先在本地全量跑 backend/tests 再提界。
numpy>=2.5,<3
onnx>=1.22,<2
onnxconverter-common>=1.16,<2
onnxruntime-directml>=1.24.4,<2
pillow>=12.3,<13
psutil>=7.2,<8
rich>=15.0,<16
pytest>=9.1,<10
fastapi>=0.141,<0.142
# starlette 1.6 的 TestClient 依赖 httpx2（不是 httpx 的笔误，删了测试收集就红）
httpx2>=2.12,<3
uvicorn>=0.52,<0.53
websockets>=17.0,<18
py7zr>=1.1.3,<2
